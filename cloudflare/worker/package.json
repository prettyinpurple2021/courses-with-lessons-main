{
  "name": "intel-academy-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:generate": "drizzle-kit generate --config ./drizzle.config.ts",
    "db:migrate": "wrangler d1 migrations apply DB --config ./wrangler.env.toml"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.10",
    "hono": "^4.5.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "drizzle-kit": "^0.25.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.47.0"
  }
}
